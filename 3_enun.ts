enum Frameworks {
  React,
  Angular,
  Vue
}

const framework = Frameworks[1];
console.log(framework);

enum SocialMedia {
  VK = 'vk',
  FACEBOOK = 'facebook'
}

const media = SocialMedia.FACEBOOK;
